{
  "name": "tax-payment-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply tax-payment-manager-db --local"
  },
  "dependencies": {
    "@google/genai": "^1.50.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240405.0",
    "drizzle-kit": "^0.31.10",
    "wrangler": "^4.83.0"
  }
}
